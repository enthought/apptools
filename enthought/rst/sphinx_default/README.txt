**The content here is current as of Sphinx version 0.6.1.**

This directory contains the default style sheets and image resources that 
Sphinx generates (i.e. the default contents of the "_static" directory).

For the tools in this package that generate HTML using Sphinx, this directory
is used as the default location for Sphinx resources.
